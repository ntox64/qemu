# SPDX-License-Identifier: BSD-2-Clause

# scsitest clean bring-up: no error rules.
# Chain is raw -> blkdebug -> file (format on top of blkdebug so the
# format driver's read_aio/write_aio events land on the blkdebug node).
