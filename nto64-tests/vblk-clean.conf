# SPDX-License-Identifier: BSD-2-Clause

# vblktest clean bring-up: no error rules.
# The chain is raw -> blkdebug -> file (format on top of blkdebug so the
# format driver's read_aio/write_aio/flush_to_os events land on blkdebug).
